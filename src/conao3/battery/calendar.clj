(ns conao3.battery.calendar)

(defn firstweekday
  [& _args]
  (throw (ex-info "Not implemented" {})))

(defn setfirstweekday
  [& _args]
  (throw (ex-info "Not implemented" {})))

(defn isleap
  [& _args]
  (throw (ex-info "Not implemented" {})))

(defn monthrange
  [& _args]
  (throw (ex-info "Not implemented" {})))

(defn leapdays
  [& _args]
  (throw (ex-info "Not implemented" {})))

(defn timegm
  [& _args]
  (throw (ex-info "Not implemented" {})))

(defn format
  [& _args]
  (throw (ex-info "Not implemented" {})))

(defn main
  [& _args]
  (throw (ex-info "Not implemented" {})))
