(ns conao3.battery.glob)

(defn glob
  ([pattern]
   (throw (ex-info "Not implemented" {})))
  ([pattern _opts]
   (throw (ex-info "Not implemented" {})))
  ([pattern _opts & _more]
   (throw (ex-info "Not implemented" {}))))

(defn iglob
  ([pattern]
   (throw (ex-info "Not implemented" {})))
  ([pattern _opts]
   (throw (ex-info "Not implemented" {})))
  ([pattern _opts & _more]
   (throw (ex-info "Not implemented" {}))))

(defn escape
  ([value]
   (throw (ex-info "Not implemented" {})))
  ([value _opts]
   (throw (ex-info "Not implemented" {}))))

(defn translate
  ([pattern]
   (throw (ex-info "Not implemented" {})))
  ([pattern _opts]
   (throw (ex-info "Not implemented" {})))
  ([pattern _opts & _more]
   (throw (ex-info "Not implemented" {}))))
