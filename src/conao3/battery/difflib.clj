(ns conao3.battery.difflib)

(defn sequence-matcher [& args]
  (throw (ex-info "Not implemented" {})))

(defn sequence-matcher-ratio [& args]
  (throw (ex-info "Not implemented" {})))

(defn sequence-matcher-quick-ratio [& args]
  (throw (ex-info "Not implemented" {})))

(defn sequence-matcher-real-quick-ratio [& args]
  (throw (ex-info "Not implemented" {})))

(defn sequence-matcher-bpopular [& args]
  (throw (ex-info "Not implemented" {})))

(defn sequence-matcher-get-opcodes [& args]
  (throw (ex-info "Not implemented" {})))

(defn sequence-matcher-bjunk [& args]
  (throw (ex-info "Not implemented" {})))

(defn sequence-matcher-get-matching-blocks [& args]
  (throw (ex-info "Not implemented" {})))

(defn sequence-matcher-get-grouped-opcodes [& args]
  (throw (ex-info "Not implemented" {})))

(defn sequence-matcher-find-longest-match [& args]
  (throw (ex-info "Not implemented" {})))

(defn differ [& args]
  (throw (ex-info "Not implemented" {})))

(defn differ-compare [& args]
  (throw (ex-info "Not implemented" {})))

(defn html-diff [& args]
  (throw (ex-info "Not implemented" {})))

(defn html-diff-make-file [& args]
  (throw (ex-info "Not implemented" {})))

(defn html-diff-make-table [& args]
  (throw (ex-info "Not implemented" {})))

(defn unified-diff [& args]
  (throw (ex-info "Not implemented" {})))

(defn context-diff [& args]
  (throw (ex-info "Not implemented" {})))

(defn diff-bytes [f a b & rest]
  (throw (ex-info "Not implemented" {})))

(defn get-close-matches [& args]
  (throw (ex-info "Not implemented" {})))

(defn restore [& args]
  (throw (ex-info "Not implemented" {})))

(defn is-line-junk [& args]
  (throw (ex-info "Not implemented" {})))

(defn is-character-junk [& args]
  (throw (ex-info "Not implemented" {})))

(defn next-value [& args]
  (throw (ex-info "Not implemented" {})))
